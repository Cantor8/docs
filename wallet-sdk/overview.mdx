---
title: "Overview"
---

`C8WalletProvider` handles the full wallet lifecycle — connection, instrument and account resolution, transfer submission, and status polling — without requiring dApp developers to manage Canton-specific cryptography or ledger communication directly. All wallet interaction happens through a secure popup window using the browser's native `postMessage` API.